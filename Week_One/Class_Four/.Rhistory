flowerVec <- c(flower1, flower2, flower3)
MeanFlowers[j] <- flowerVec
}
MeanFlowers
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
# Loop
for (j in measurements) {
flower1<- mean(subsetflower1[, j])
flower2<- mean(subsetflower2[, j])
flower3<- mean(subsetflower3[, j])
flowerVec[j] <- c(flower1, flower2, flower3)
MeanFlowers[j] <- flowerVec[j]
}
MeanFlowers
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
# Loop
for (j in measurements) {
flower1<- mean(subsetflower1[, j])
flower2<- mean(subsetflower2[, j])
flower3<- mean(subsetflower3[, j])
flowerVec <- c(flower1, flower2, flower3)
MeanFlowers[j] <- flowerVec
}
MeanFlowers
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
# Loop
for (j in measurements) {
flower1<- mean((subsetflower1[, j]))
flower2<- mean((subsetflower2[, j]))
flower3<- mean((subsetflower3[, j]))
flowerVec <- c(flower1, flower2, flower3)
MeanFlowers[j] <- flowerVec
}
MeanFlowers
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
# Loop
for (j in measurements) {
flower1<- mean((subsetflower1[, j]))
flower2<- mean((subsetflower2[, j]))
flower3<- mean((subsetflower3[, j]))
flowerVec[j] <- c(flower1, flower2, flower3)
MeanFlowers[j] <- flowerVec
}
MeanFlowers
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
# Loop
for (j in measurements) {
flower1<- mean((subsetflower1[, j]))
flower2<- mean((subsetflower2[, j]))
flower3<- mean((subsetflower3[, j]))
MeanFlowers[j] <- c(flower1, flower2, flower3)
}
MeanFlowers
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
# Loop
for (j in measurements) {
flower1<- mean((subsetflower1[ j]))
flower2<- mean((subsetflower2[ j]))
flower3<- mean((subsetflower3[ j]))
MeanFlowers[j] <- c(flower1, flower2, flower3)
}
MeanFlowers
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
# Loop
for (j in measurements) {
flower1<- mean((subsetflower1[, j]))
flower2<- mean((subsetflower2[, j]))
flower3<- mean((subsetflower3[, j]))
MeanFlowers[j] <- c(flower1, flower2, flower3)
}
MeanFlowers
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
# Loop
for (j in measurements) {
flower1<- mean((subsetflower1[, j]))
flower2<- mean((subsetflower2[, j]))
flower3<- mean((subsetflower3[, j]))
MeanFlowers[j] <- c(flower1, flower2, flower3)
print(MeanFlowers[j])
}
MeanFlowers
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
# Loop
for (j in measurements) {
print(j)
flower1<- mean((subsetflower1[, j]))
flower2<- mean((subsetflower2[, j]))
flower3<- mean((subsetflower3[, j]))
MeanFlowers[j] <- c(flower1, flower2, flower3)
print(MeanFlowers[j])
}
MeanFlowers
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
mean((subsetflower1[,"Sepal.Length"]))
mean((subsetflower2[,"Sepal.Length"]))
mean((subsetflower3[,"Sepal.Length"]))
# Loop
for (j in measurements) {
print(j)
flower1<- mean((subsetflower1[, j]))
flower2<- mean((subsetflower2[, j]))
flower3<- mean((subsetflower3[, j]))
MeanFlowers[j] <- c(flower1, flower2, flower3)
print(MeanFlowers[j])
}
MeanFlowers
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
mean((subsetflower1[,"Sepal.Length"]))
mean((subsetflower2[,"Sepal.Length"]))
mean((subsetflower3[,"Sepal.Length"]))
# Loop
for (j in measurements) {
print(j)
flower1<- mean((subsetflower1[, j]))
flower2<- mean((subsetflower2[, j]))
flower3<- mean((subsetflower3[, j]))
vec <-  c(flower1, flower2, flower3)
print(MeanFlowers[j])
}
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
mean((subsetflower1[,"Sepal.Length"]))
mean((subsetflower2[,"Sepal.Length"]))
mean((subsetflower3[,"Sepal.Length"]))
# Loop
for (j in measurements) {
print(j)
flower1<- mean((subsetflower1[, j]))
flower2<- mean((subsetflower2[, j]))
flower3<- mean((subsetflower3[, j]))
vec <-  c(flower1, flower2, flower3)
print(vec)
print(MeanFlowers[j])
}
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
mean((subsetflower1[,"Sepal.Length"]))
mean((subsetflower2[,"Sepal.Length"]))
mean((subsetflower3[,"Sepal.Length"]))
# Loop
for (j in measurements) {
flower1<- mean((subsetflower1[, j]))
flower2<- mean((subsetflower2[, j]))
flower3<- mean((subsetflower3[, j]))
vec <-  c(flower1, flower2, flower3)
print(MeanFlowers$j)
}
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
mean((subsetflower1[,"Sepal.Length"]))
mean((subsetflower2[,"Sepal.Length"]))
mean((subsetflower3[,"Sepal.Length"]))
# Loop
for (j in measurements) {
flower1<- mean((subsetflower1[, j]))
flower2<- mean((subsetflower2[, j]))
flower3<- mean((subsetflower3[, j]))
vec <-  c(flower1, flower2, flower3)
print(MeanFlowers[j])
}
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
mean((subsetflower1[,"Sepal.Length"]))
mean((subsetflower2[,"Sepal.Length"]))
mean((subsetflower3[,"Sepal.Length"]))
# Loop
for (j in measurements) {
flower1<- mean((subsetflower1[, j]))
flower2<- mean((subsetflower2[, j]))
flower3<- mean((subsetflower3[, j]))
vec <-  c(flower1, flower2, flower3)
print(MeanFlowers[1])
}
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
mean((subsetflower1[,"Sepal.Length"]))
mean((subsetflower2[,"Sepal.Length"]))
mean((subsetflower3[,"Sepal.Length"]))
# Loop
for (j in measurements) {
flower1<- mean((subsetflower1[, j]))
flower2<- mean((subsetflower2[, j]))
flower3<- mean((subsetflower3[, j]))
vec <-  c(flower1, flower2, flower3)
print(MeanFlowers["Sepal.Length"])
}
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
mean((subsetflower1[,"Sepal.Length"]))
mean((subsetflower2[,"Sepal.Length"]))
mean((subsetflower3[,"Sepal.Length"]))
# Loop
for (j in measurements) {
flower1<- mean((subsetflower1[, j]))
flower2<- mean((subsetflower2[, j]))
flower3<- mean((subsetflower3[, j]))
vec <-  c(flower1, flower2, flower3)
print(MeanFlowers[Sepal.Length])
}
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
mean((subsetflower1[,"Sepal.Length"]))
mean((subsetflower2[,"Sepal.Length"]))
mean((subsetflower3[,"Sepal.Length"]))
# Loop
for (j in measurements) {
flower1<- mean((subsetflower1[, j]))
flower2<- mean((subsetflower2[, j]))
flower3<- mean((subsetflower3[, j]))
vec <-  c(flower1, flower2, flower3)
print(MeanFlowers[, Sepal.Length])
}
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
mean((subsetflower1[,"Sepal.Length"]))
mean((subsetflower2[,"Sepal.Length"]))
mean((subsetflower3[,"Sepal.Length"]))
# Loop
for (j in measurements) {
flower1<- mean((subsetflower1[, j]))
flower2<- mean((subsetflower2[, j]))
flower3<- mean((subsetflower3[, j]))
vec <-  c(flower1, flower2, flower3)
print(MeanFlowers[, "Sepal.Length"])
}
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
mean((subsetflower1[,"Sepal.Length"]))
mean((subsetflower2[,"Sepal.Length"]))
mean((subsetflower3[,"Sepal.Length"]))
# Loop
for (j in measurements) {
flower1<- mean((subsetflower1[, j]))
flower2<- mean((subsetflower2[, j]))
flower3<- mean((subsetflower3[, j]))
vec <-  c(flower1, flower2, flower3)
print(MeanFlowers["Sepal.Length", ])
}
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
mean((subsetflower1[,"Sepal.Length"]))
mean((subsetflower2[,"Sepal.Length"]))
mean((subsetflower3[,"Sepal.Length"]))
# Loop
for (j in measurements) {
flower1<- mean((subsetflower1[, j]))
flower2<- mean((subsetflower2[, j]))
flower3<- mean((subsetflower3[, j]))
vec <-  c(flower1, flower2, flower3)
MeanFlowers[j, ] <- vec
}
# define a matrix of zeros
MeanFlowers <- matrix(0,nrow=4,ncol=3)
# define a character vector corresponding to the numeric variable names
measurements <- c("Sepal.Length","Sepal.Width","Petal.Length","Petal.Width")
# name the rows and columns of the matrix MeanFlowers
rownames(MeanFlowers)  <- measurements
colnames(MeanFlowers)  <- c("setosa","versicolor","virginica")
#subsetting flowers
subsetflower1 <- subset(iris, iris$Species == "setosa")
subsetflower2 <- subset(iris, iris$Species == "versicolor")
subsetflower3 <- subset(iris, iris$Species == "virginica")
# Loop
for (j in measurements) {
flower1<- mean((subsetflower1[, j]))
flower2<- mean((subsetflower2[, j]))
flower3<- mean((subsetflower3[, j]))
vec <-  c(flower1, flower2, flower3)
MeanFlowers[j, ] <- vec
}
MeanFlowers
## head function
head(iris)
## names function
names(iris)
Versicolor <- ifelse(iris$Species == "versicolor", 1, 0)
# Don't forget to uncomment the code below
boxplot(Sepal.Width~Versicolor,main="Comparing Sepal Width of Two Species",ylab="sepal width",data=iris)
mean(iris$Sepal.Length[iris$Species == "setosa"])
tapply(iris$Sepal.Length,iris$Species,mean)
setwd("~/Desktop/Data")
Grocery <- read.table("Kutner_6_9.txt", header=T)
head(Grocery)
lm0 <- lm(Y ~ X1 + X2 + X3, data = Grocery)
residuals(lm0)[1:5]
fitted(lm0)[1:5]
lm0 <- lm(Y ~ X1 + X2 + X3, data = Grocery)
lm0 <- lm(Y ~ X1 + X2 + X3, data = Grocery)
lm0 <- lm(Y ~ X1 + X2 + X3, data = Grocery)
lm0
2*v1+v2
v1 <- c(3,-2,7)
v2 <- c(-6,4,-14)
2*v1+v2
head(Grocery)
sum(residuals(lm0))
sum(fitted(lm0))
sum(Grocery$Y)
# Define X_4
X4 <- ifelse(Grocery$X3==1,0,1)
# Define redundant design matrix
X <- cbind(rep(1,52), Grocery$X1, Grocery$X2, Grocery$X3,X4)
head(X)
qr(t(X) %*% X)$rank
det(t(X) %*% X)
solve((t(X) %*% X))
qr(t(X) %*% X)$rank
det(t(X) %*% X)
solve((t(X) %*% X))
lm(Y~X1+X2+X3,data=Grocery)
lm(Y~X1+X2+X3+X4,data=Grocery)
lm(Y~X1+X2+X3,data=Grocery)
